# Best Practices

Bewährte Methoden und Praktiken für erfolgreiche Arbeit.

## 📋 Allgemeine Prinzipien

### 1. **Klarheit vor Cleverness**

Code und Dokumentation sollten zunächst klar sein, bevor sie clever sind.

```typescript
// ✅ Gut: Klar und verständlich
function calculateTotalPrice(items: Item[]): number {
  return items.reduce((sum, item) => sum + item.price, 0)
}

// ❌ Schlecht: Zu clever
const p = (i: any[]) => i.reduce((s, x) => s + x.p, 0)
```

### 2. **DRY - Don't Repeat Yourself**

Vermeide wiederholten Code durch Abstraktion:

```typescript
// ✅ Gut: Wiederverwendbar
function formatCurrency(amount: number): string {
  return new Intl.NumberFormat('de-DE', {
    style: 'currency',
    currency: 'EUR',
  }).format(amount)
}

// Verwende überall
const price = formatCurrency(99.99)
const total = formatCurrency(sum)
```

### 3. **KISS - Keep It Simple, Stupid**

Halte Lösungen so einfach wie möglich:

```typescript
// ✅ Einfach und effektiv
const isValid = (email: string) => email.includes('@')

// ❌ Überverkompliziert
const isValid = (email: string) => {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  return regex.test(email)
}
```

## 🔒 Sicherheit

### Umgang mit Secrets

> **Wichtig**: Speichere niemals Secrets im Code oder in Git!

```bash
# ✅ Richtig: Verwende Environment Variables
DISCORD_CLIENT_SECRET=xxx

# ❌ Falsch: Secrets im Code
const secret = "xyz123abc456"
```

### Eingabe-Validierung

Validiere alle Eingaben:

```typescript
// ✅ Mit Validierung
function createUser(email: string, name: string) {
  if (!email.includes('@')) throw new Error('Invalid email')
  if (!name.trim()) throw new Error('Name required')
  // Erstelle User...
}
```

## 📝 Code-Dokumentation

### Comments schreiben

```typescript
/**
 * Berechnet die Gesamtsumme aller Items
 *
 * @param items - Array von Items mit Preisen
 * @returns Die Gesamtsumme
 *
 * @example
 * const items = [{price: 10}, {price: 20}]
 * console.log(sum(items)) // 30
 */
function sum(items: Array<{ price: number }>): number {
  return items.reduce((acc, item) => acc + item.price, 0)
}
```

## 🚀 Performance

### Lazy Loading

Lade Ressourcen nur bei Bedarf:

```typescript
// ✅ Lazy Loading
const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <p>Loading...</p>,
})
```

### Caching

Nutze Caching für häufig abgerufene Daten:

```typescript
// ✅ Mit einfachem Cache
const cache = new Map()

function getData(key: string) {
  if (cache.has(key)) {
    return cache.get(key)
  }
  const data = fetchExpensiveData(key)
  cache.set(key, data)
  return data
}
```

## ✅ Checkliste vor dem Deployment

- [ ] Code wurde reviewed
- [ ] Tests sind grün
- [ ] Keine Console-Fehler in Production
- [ ] Secrets sind in .env.local
- [ ] Performance ist optimiert
- [ ] Mobile-Responsive
- [ ] Accessibility geprüft
- [ ] SEO-Grundlagen umgesetzt

---

Diese Best Practices helfen, qualitativ hochwertigen und wartbaren Code zu schreiben. 🎯
