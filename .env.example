# ===================================================================
# SIF Wissensdatenbank - Environment Variables
# ===================================================================
# WICHTIG: Kopiere diese Datei zu .env.local und fülle sie mit echten Werten aus
# NIEMALS .env.local in Git committen!
# ===================================================================

# ===================================================================
# Discord OAuth Configuration (ERFORDERLICH)
# ===================================================================

# Discord Application Client ID
# Besorge von: https://discord.com/developers/applications
# 1. Klicke "New Application"
# 2. Gib einen Namen ein (z.B. "Wissensdatenbank")
# 3. Gehe zu "OAuth2" → "General"
# 4. Kopiere die "Client ID"
DISCORD_CLIENT_ID=1437217754913706175

# Discord Application Client Secret
# Vorsicht: Niemals in Code committen!
# Von: https://discord.com/developers/applications → Deine App → OAuth2 → General
# Klicke auf "Reset Secret" wenn du ihn verloren hast
DISCORD_CLIENT_SECRET=xUfFvUH8b10cJ4ihamrl391iTcig-uEs

# ===================================================================
# NextAuth.js Configuration (ERFORDERLICH)
# ===================================================================

# Secret zur Verschlüsselung von Sessions und JWT-Token
# Generiere mit: openssl rand -base64 32
# Unter Windows (PowerShift): [Convert]::ToBase64String([System.Random]::new().NextBytes(32))
NEXTAUTH_SECRET=your_nextauth_secret_here

# URL der Anwendung (wird in OAuth-Callbacks verwendet)
# Development: http://localhost:3000
# Production: https://yourdomain.com (HTTPS erforderlich!)
NEXTAUTH_URL=http://localhost:3000

# Optional: Explizites NEXTAUTH_URL_INTERNAL (nur wenn proxy-Setup notwendig)
# NEXTAUTH_URL_INTERNAL=http://localhost:3000

# ===================================================================
# Optional: Analytics & Monitoring
# ===================================================================

# Google Analytics (optional)
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Sentry Error Tracking (optional)
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ===================================================================
# Optional: Weitere Features
# ===================================================================

# Database URL (falls Datenbank später hinzugefügt wird)
# DATABASE_URL=postgresql://user:password@localhost:5432/wissensdatenbank

# API Keys für externe Services
# API_KEY=your_api_key

# ===================================================================
# Notizen für Setup
# ===================================================================
# 1. Discord OAuth Setup:
#    - Gehe zu https://discord.com/developers/applications
#    - Erstelle eine neue Application
#    - Unter OAuth2 → General: Kopiere Client ID und Client Secret
#    - Unter OAuth2 → Redirects:
#      * http://localhost:3000/api/auth/callback/discord (Development)
#      * https://yourdomain.com/api/auth/callback/discord (Production)
#
# 2. NEXTAUTH_SECRET generieren:
#    - macOS/Linux: openssl rand -base64 32
#    - Windows PS: [Convert]::ToBase64String([System.Random]::new().NextBytes(32))
#
# 3. .env.local erstellen:
#    - Kopiere diese Datei zu .env.local
#    - Fülle alle erforderlichen Variablen aus
#    - Speichere .env.local in .gitignore
#
# 4. Deployment (Vercel/Railway):
#    - Setze diese Variablen in Dashboard
#    - NEXTAUTH_URL muss mit deiner Production URL übereinstimmen
#    - NEXTAUTH_URL muss HTTPS sein für Production!
# ===================================================================
